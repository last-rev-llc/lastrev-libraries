query SitemapPage($contentType: String!, $locale: String!, $preview: Boolean, $site: String, $page: Int!) {
  sitemapPage(contentType: $contentType, locale: $locale, preview: $preview, site: $site, page: $page) {
    entries {
      loc
      lastmod
    }
  }
}
