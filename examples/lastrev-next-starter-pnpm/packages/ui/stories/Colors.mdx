import { Meta, Typeset, Story, ColorPalette, ColorItem } from '@storybook/blocks';
import { linkTo } from '@storybook/addon-links';
import { theme, mainColors } from '../src/ThemeRegistry/theme';

<Meta
  title="1. Brand/Colors"
  parameters={{
    previewTabs: {
      'storybook/docs/panel': { hidden: true }
    },
    options: { isToolshown: false }
  }}
/>

# Colors

### Primary palette

<ColorPalette>
  {Object.keys(theme.palette).map((palette) =>
    theme.palette[palette]?.main && mainColors.includes(palette) ? (
      <ColorItem title={palette.toUpperCase()} subtitle={palette} colors={theme.palette[palette]} />
    ) : null
  )}
</ColorPalette>

### Accent palette

<ColorPalette>
  {Object.keys(theme.palette).map((palette) =>
    !!theme.palette[palette]?.main && !mainColors.includes(palette) ? (
      <ColorItem title={palette.toUpperCase()} subtitle={palette} colors={theme.palette[palette]} />
    ) : null
  )}
</ColorPalette>

### Other

<ColorPalette>
  {Object.keys(theme.palette).map((palette) =>
    !theme.palette[palette]?.main && typeof theme.palette[palette] === 'object' ? (
      <ColorItem title={palette.toUpperCase()} subtitle={palette} colors={theme.palette[palette]} />
    ) : null
  )}
</ColorPalette>
