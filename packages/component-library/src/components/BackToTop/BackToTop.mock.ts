import { BackToTopProps } from './BackToTop'

const mockContent = (): BackToTopProps => {
  return {
    FabProps: {
      size: 'large',
      color: 'primary',
      disabled: true
    }
  };
};

export default mockContent;
